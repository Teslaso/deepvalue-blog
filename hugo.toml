baseURL = "https://deepvalue.space/"
languageCode = "en-us"
title = "DEEP VALUE"
description = "mingxuan's blog"
theme = ["book"]

# 分类配置
[taxonomies]
  category = "categories"
  tag = "tags"

[params]
  mainSections = ["posts"]
  description = "Searching for Asymmetry in Markets"
  # Legacy search config from book theme (optional to keep or remove)
  BookSearch = true
  BookDateFormat = "2006-01-02"
  BookSearchConfig = '{"threshold": 0.2, "distance": 100, "includeScore": true, "keys": ["title", "content", "tags", "categories"]}'